<!DOCTYPE html>
<html lang="id">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error | Smart Garden IoT</title>

  <!-- Bootstrap CSS from CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

  <style>
    :root {
      --primary: #2ecc71;
      --secondary: #27ae60;
      --dark: #1e8449;
      --light: #d5f5e3;
      --accent: #3498db;
      --danger: #e74c3c;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f8f9fa;
      background-image: url('https://cdn.jsdelivr.net/gh/tailwindcss/heropatterns@0.3.1/topography.svg');
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    .error-container {
      max-width: 600px;
      text-align: center;
    }

    .error-icon {
      font-size: 5rem;
      color: var(--danger);
      margin-bottom: 1rem;
    }

    .error-code {
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 1rem;
    }

    .error-message {
      font-size: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .error-details {
      background-color: rgba(255, 255, 255, 0.7);
      padding: 1rem;
      border-radius: 10px;
      margin-bottom: 1.5rem;
      text-align: left;
    }

    .back-button {
      background-color: var(--primary);
      border-color: var(--primary);
    }

    .back-button:hover {
      background-color: var(--secondary);
      border-color: var(--secondary);
    }
  </style>
</head>

<body>
  <div class="error-container">
    <div class="error-icon">
      <i class="fas fa-exclamation-triangle"></i>
    </div>

    <div class="error-code">500</div>

    <div class="error-message">Server Error</div>

    <div class="error-details">
      <p>Mohon maaf, terjadi kesalahan pada server:</p>
      <pre class="bg-light p-3 rounded"><%= error.message %></pre>

      <% if (process.env.NODE_ENV !== 'production') { %>
      <div class="mt-3">
        <p><strong>Stack Trace:</strong></p>
        <pre class="bg-light p-3 rounded small" style="max-height: 200px; overflow-y: auto;"><%= error.stack %></pre>
      </div>
      <% } %>
    </div>

    <div class="d-flex justify-content-center">
      <a href="/dashboard" class="btn btn-primary back-button me-2">
        <i class="fas fa-tachometer-alt me-2"></i>
        Kembali ke Dashboard
      </a>

      <a href="/" class="btn btn-outline-secondary">
        <i class="fas fa-home me-2"></i>
        Kembali ke Beranda
      </a>
    </div>
  </div>

  <!-- Bootstrap JS from CDN -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>